syntax = "proto3";

package rpc;

option go_package = "git.17zjh.com/snake/snake_proto/rpc";

enum cmd {
  Undef = 0;
  VoiceRoom = 1;
  Music = 2;
  SnakeApi = 3;
  Activity = 4; // 活动服
  System = 5; // 系统功能服
  Master = 6; // 中心服（仅服务器使用）
  Gate = 7; // 网关服（仅服务器使用)
  UnitySvcCtl = 8; // unity server控制服务（仅服务器使用)
  Match = 9; // 匹配服（仅服务器使用）
  Matchx = 10; // 新匹配服
}
